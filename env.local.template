# ============================================
# PixelVault Environment Configuration
# ============================================
# Copy this file to .env.local and update with your actual values
# Command: cp env.local.template .env.local
# DO NOT commit .env.local to git (it's in .gitignore)

# ============================================
# Database Configuration (REQUIRED)
# ============================================
# Local PostgreSQL Database Settings
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=ImageStorage
DATABASE_USER=postgres
DATABASE_PASSWORD=your_postgres_password_here

# SSL Configuration
# Set to 'false' for localhost connections (no SSL needed)
# Set to 'true' for remote/cloud databases (like Google Cloud)
DATABASE_SSL=false

# ============================================
# Database Connection Pool Settings (OPTIONAL)
# ============================================
# Connection timeout in milliseconds (default: 10000 = 10 seconds)
# Lower values = faster failure detection, higher = more tolerance for slow connections
DATABASE_CONNECTION_TIMEOUT=10000

# Statement timeout in milliseconds (default: 120000 = 2 minutes)
# Maximum time a query can run before being cancelled
DATABASE_STATEMENT_TIMEOUT=120000

# Connection pool settings (configured in code, but documented here)
# min: 5 connections (minimum pool size)
# max: 20 connections (maximum pool size)
# idleTimeout: 30 seconds (close idle connections)

# ============================================
# Application Environment (OPTIONAL)
# ============================================
# Environment mode: 'development', 'staging', or 'production'
NODE_ENV=development

# ============================================
# Next.js Public API URL (OPTIONAL)
# ============================================
# Public API URL for frontend (default: http://localhost:3000)
# Only needed if your API is on a different domain
NEXT_PUBLIC_API_URL=http://localhost:3000

# ============================================
# Remote Database Configuration (OLD - FOR REFERENCE)
# ============================================
# If you need to migrate from a remote database, use these temporarily:
# DATABASE_HOST=your_remote_database_host
# DATABASE_PORT=5432
# DATABASE_NAME=postgres
# DATABASE_USER=postgres
# DATABASE_PASSWORD=your_remote_password_here
# DATABASE_SSL=true

# ============================================
# Notes:
# ============================================
# 1. Replace 'your_postgres_password_here' with your actual PostgreSQL password
# 2. For localhost, DATABASE_SSL should be 'false'
# 3. Database name 'ImageStorage' should match exactly what you created in pgAdmin
#    (PostgreSQL is case-sensitive if you used quotes, otherwise it's lowercase)
# 4. Default PostgreSQL port is 5432
# 5. Default PostgreSQL user is 'postgres' (unless you created a different user)
# 6. After updating this file, restart your development server
